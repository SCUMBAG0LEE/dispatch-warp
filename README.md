# dispatch-warp
🚀 Next-generation multi-interface load balancer in Golang. Combines Wi-Fi, Ethernet, and Cellular connections into a single SOCKS5 proxy while routing each path through a unique, secure Cloudflare WARP connection.
